import { Box, Typography } from '@mui/material';

const Canvas: React.VFC = () => {
    return (
        <Box sx={{ width: '100%', minHeight: '100%', backgroundColor: '#ffffff' }}>
            <Typography variant="caption">canvas</Typography>
        </Box>
    );
};

export default Canvas;
